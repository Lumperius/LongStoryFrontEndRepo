/*
import { React } from 'react';
import { connect } from 'react-redux';

const CheckAuth = (component, roles) =>{
 return class AuthCheck extends React.Component{
     constructor(){
         super();
         this.state = {
         havePermission: flase
         }
     }

     checkAuth() {
         userRole = this.props.role
         if(userRole == role)
     }
 }  
}

mapStatesToProps() = function(){
  return{
      role: state.role
  }
}

export default connect(mapStatesToProps)(CheckAuth)
*/ //TODO
